{
  "name": "functions",
  "description": "Firebase Cloud Functions for Chariow webhook",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "src/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^4.7.0",
    "express": "^4.19.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
